This Python program uses the curses library to visually display and solve a maze in the terminal. 
The maze is represented as a 2D list where walls are indicated by "#", the start position is "O", and the end position is "X". 
The program finds the path from the start to the end using a breadth-first search algorithm and displays the process step-by-step.
